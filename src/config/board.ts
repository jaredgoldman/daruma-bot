/**
 * Mess with these settubgs to change the board size
 */
export const boardConfig = {
  board: {
    roundWidth: 12,
    // 12 / 3 = 4
    cellWidth: 4,
    centeredLength: 9,
    roundPadding: 1,
  },
}
